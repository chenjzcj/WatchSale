apply plugin: 'com.android.application'

android {
    signingConfigs {
        keystore {
            keyAlias 'ruanan'
            keyPassword '2015ruanan'
            storeFile file('D:/Projects/ZCJWorkSpace/WatchSale/WatchSale/app/key/chq.keystore')
            storePassword '2015ruanan'
        }
    }
    compileSdkVersion 23
    buildToolsVersion '26.0.2'
    defaultConfig {
        applicationId "com.ran.watchsale"
        minSdkVersion 17
        targetSdkVersion 23
        versionCode 2
        versionName "1.0.1"
        signingConfig signingConfigs.keystore
    }
    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
        }
    }
    productFlavors {
    }
}

dependencies {
    compile fileTree(include: ['*.jar'], dir: 'libs')
    testCompile 'junit:junit:4.12'
    compile 'com.android.support:appcompat-v7:23.4.0'
}
